#pragma once

struct employee;
namespace MySpace
{
	void arrInit(float*, const size_t);
	void arrCout(const float*, const size_t);
	void arrTask(const float*, const size_t);
}
void arr2Init(int* , const size_t);
void arrSort(int*, const size_t);
void arrCout(const int* , const size_t );
void structTask();